{
  "name": "restate-xstate",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm -F web dev",
    "dev:admin": "pnpm -F admin dev",
    "dev:api": "pnpm -F api dev",
    "dev:all": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "vitest",
    "db:seed": "pnpm -F db seed",
    "db:seed:workflows": "pnpm -F db seed:workflows",
    "restate:up": "docker compose up -d"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
